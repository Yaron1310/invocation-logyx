{
  "mcpServers": {
    "invocation": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/hub.js"],
      "env": {
        "INVOCATION_SITES_FILE": "${INVOCATION_SITES_FILE:-}"
      }
    }
  }
}
