// Use the WordPress Prettier config (wp-prettier) that @wordpress/scripts ships with,
// so editor-on-save formatting matches `npm run lint:js` / `npm run format` exactly.
module.exports = require( '@wordpress/prettier-config' );
